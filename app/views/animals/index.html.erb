
<div class="w-full">
  <div class="flex justify-between bg-white items-center rounded-lg p-4">
    <div>
      <h1 class="text-3xl md:text-4xl font-bold">🐶 Animais</h1>
    </div>
    <div>
      <%= link_to "Adicionar animal", new_animal_path, class: "bg-black text-white md:p-2 text-xs p-1.5 rounded-lg" %>
    </div>
  </div>
  <div class="md:flex md:justify-beetween md:items-center ">
    <div class="hidden md:block">
      <%= paginate @animals %>
    </div>
    <div class="ml-auto">
      <%= render 'search_input' %>
    </div>
  </div>
    <%= render 'table' %>
  <div class="flex justify-center md:hidden">
    <%= paginate @animals, window: 1 %>
  </div>
</div>
