
<div class="w-full">
  <div class="flex justify-between bg-white items-center rounded-lg p-4">
    <div>
      <h1 class="md:text-4xl font-bold ">💉 Medicamentos</h1>
    </div>
    <div>
      <%= link_to "Adicionar medicamento", new_medicine_path, class: "bg-black text-white md:p-2 text-xs p-1.5 rounded-lg" %>
    </div>
  </div>
  <div class="md:flex md:justify-beetween md:items-center">
    <div class="hidden md:block">
        <%= paginate @medicines %>
    </div>
    <div class="md:ml-auto flex justify-center">
      <%= render 'search_input' %>
    </div>
  </div>
  <%= render 'table' %>
  <div class="md:hidden">
    <%= paginate @medicines %>
  </div>
</div>
